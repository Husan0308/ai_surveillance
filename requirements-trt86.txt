# Isolated TensorRT 8.6 runtime for Pascal (GTX 1050 Ti / SM 6.1).
# Create with Python 3.10. Keep separate from the main DeepStream/project venv.
# Only packages required by the canonical SHM TensorRT inference runtime live here.
numpy>=1.24,<2
nvidia-cuda-runtime-cu12==12.1.105
nvidia-cublas-cu12==12.1.3.1
nvidia-cudnn-cu12==8.9.1.23
tensorrt==8.6.1.post1
