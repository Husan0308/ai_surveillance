# Isolated TensorRT 8.6 environment for Pascal (GTX 1050 Ti / SM 6.1).
# Create this with Python 3.10. Do not install into the DeepStream/project venv.
# TensorRT 8.6.1 is intentionally pinned because newer TensorRT releases drop Pascal.
# Keep the CUDA/cuDNN runtime aligned with the TensorRT 8.6 support matrix instead
# of inheriting the host DeepStream/TensorRT 10/11 runtime.
numpy>=1.24,<2
onnx>=1.17,<2
nvidia-cuda-runtime-cu12==12.1.105
nvidia-cublas-cu12==12.1.3.1
nvidia-cudnn-cu12==8.9.1.23
tensorrt==8.6.1.post1
