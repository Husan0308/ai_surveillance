# Camera V2 person detector. Keep the existing project PyTorch/CUDA install;
# install this file into that same environment rather than replacing torch.
# supervision/OpenCV in this deployment must stay on the NumPy 1.x ABI.
numpy>=1.26,<2
scipy>=1.17,<1.18
opencv-python-headless>=4.10,<4.12
rfdetr>=1.9.3,<2
onnx>=1.17,<2
